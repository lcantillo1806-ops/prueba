Este proyecto utiliza Maven para la construcción de microservicios y Docker Compose para el despliegue de los contenedores.

------------------------------------------------------------
🚀 Instrucciones de ejecución
------------------------------------------------------------

1) Requisitos previos:
- Tener Docker Desktop abierto.
- Tener Node.js y Angular CLI instalados si se levantará el frontend.

------------------------------------------------------------
2) Levantar el microservicio producto-ms
------------------------------------------------------------

1. Ingresar al directorio:
   productos-ms/deploy

2. Abrir una consola CMD o Bash dentro de esa carpeta.

3. Ejecutar Docker Compose:
   docker compose up --build

------------------------------------------------------------
3) Levantar el microservicio inventario-ms
------------------------------------------------------------

NOTA: Esperar a que el contenedor de producto-ms esté completamente arriba antes de iniciar inventario-ms.

1. Ingresar al directorio:
   inventario-ms/deploy

2. Abrir una consola CMD o Bash dentro de esa carpeta.

3. Ejecutar Docker Compose:
   docker compose up --build

------------------------------------------------------------
4) Acceso a las APIs (Swagger)
------------------------------------------------------------

Swagger producto-ms:
http://localhost:8080/producto-ms/swagger-ui/index.html#

Swagger inventario-ms:
http://localhost:8090/inventario-sm/swagger-ui/index.html#

KeySecret requerida en Swagger:
super-secret-key

------------------------------------------------------------
5) Levantar el frontend
------------------------------------------------------------

NOTA: Esperar a que los dos microservicios estén ejecutándose.

1. Abrir consola en la carpeta raíz del frontend.
2. Ejecutar:
   ng serve --proxy-config proxy.conf.json

Frontend disponible en:
http://localhost:4200/products-admin
http://localhost:4200/products
